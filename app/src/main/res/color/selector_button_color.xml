<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo cuando está marcado -->
    <item android:color="@color/reading" android:state_checked="true" />
    <!-- Fondo cuando no está marcado -->
    <item android:color="@color/pending" android:state_checked="false"/>
</selector>